---
layout: project-layout
---
# 여행 정보 AI

| 프로젝트명 | 여행 정보 AI 시스템 구축 |
| --- | --- |
| 프로젝트기간 | 2023.05 ~ 2023.09  |
| 참여인력 | 개발자3, 기획자 1 |
| 사용한 기술스택 | Java, Elasticsearch, Spring Boot |

## 프로젝트 소개

---

![Untitled](./projects/images/%EC%97%AC%ED%96%89%EC%A0%95%EB%B3%B4AI_1.gif)

하나투어에서 제공하는 정보 이외의 여행관련 정보를 사용자에게 제공하기 위해, `ChatGPT` 를 활용하여 인공지능 챗봇을 구축한다. 

주요 목적은 사용자와 여행관련된 질의/응답에 대한 자연스럽고 의미 있는 대화를 통해 정보를 제공하고, 하나투어의 상품을 추천하며, FAQ와 같은 내부 가이드라인을 제공하고자 한다.

해당 프로젝트는 단계별로 진행이 되는 장기 프로젝트이며 현재까지는 `Phase 4`까지 구현되었다. 

### 단계별 일정

| Phase | 명칭 | 적용 일정 |
| --- | --- | --- |
| Phase 1 | OpenAI 연동 | 2023.05 |
| Phase 2 | 모바일웹 확대, 어드민 연동 (7월 12일) | 2023.07 |
| Phase 3 | NLP 연동 ( 개체명 분류, FAQ ) | 2023.07 |
| Phase 4 | FAQ (HTML, 이미지) | 2023.08 |
| Phase 5 | 패키지 상품 추천 | 미정 |

## 전체 시스템 구성

---

![Untitled](./projects/images/%EC%97%AC%ED%96%89%EC%A0%95%EB%B3%B4AI_2.png)

### 순서도

![Untitled](./projects/images/%EC%97%AC%ED%96%89%EC%A0%95%EB%B3%B4AI_3.png)

### Phase 1. OpenAI 연동

OpenAI 에서 발급받은 토큰 키값으로 프론트와 통신하는 API  프로젝트 구축.

### Phase 2. 모바일웹 확대, 어드민 연동 (7월 12일)

어드민 연동을 통한 여러 설정 값 조절 및 블랙리스트 단어 조절 기능을 추가.  `MWEB`에서도 Hanachat 서비스 오픈

> 실제 색인되는 로그 데이터를 확인하면서 여행업에 관련되지 않은 부적절한 단어가 온 경우 해당 단어를 블랙리스트에 추가함으로써 수동 관리가 가능
> 

### Phase 3. NLP 연동 ( 개체명 분류, FAQ ) - TEXT

추천 파트에서 제공하는 개체명 분류 API를 삽입하여 기존 체크 프로세스전, 여행에 관한 질문이 맞는지 판별하도록 함. 이후 FAQ에 대한 질의인지 다시 확인하여 비슷한 질의가 들어오면 하나투어에서 제공하는 FAQ 답변을 답변으로 내릴 수 있도록 구현

이때 답변은 HTML 태그를 제거한 텍스트 형태

### Phase 4. FAQ HTML 태그, 이미지 적용

FAQ답변은 여러개의 이미지와 HTML 태그들로 이루어져 있음. 정규식으로 태그를 추출하여 Response Array에 담아 태그값이 노출 될 수 있도록 구현

> Response는 `Stream형태`이므로 태그값을 추출하지 않으면 사용자에서는 `<,a,h,r,e,f` .. 형태가 보여지게 된다.
> 

## 프로젝트에 기여한 내용

---

### WebFlux Stream 방식

GPT는 Stream형태를 지원한다. RestTemplate로 구현 시에는 동기 방식이다 보니 Response데이터를 모두 받고나서 표현하게 된다. 이는 사용자로 하여금 로딩시간이 긴 것처럼 보이므로 GPT가 내려주는 Stream data를 그대로 프론트에 전달해야 한다. 

```json
// GPT Reaponse 형식
"model":"gpt-3.5-turbo-0613","choices":[{"index":0,"delta":{"content":" 중"},"finish_reason":null}]
"model":"gpt-3.5-turbo-0613","choices":[{"index":0,"delta":{"content":" 심"},"finish_reason":null}]
"model":"gpt-3.5-turbo-0613","choices":[{"index":0,"delta":{"content":" 지"},"finish_reason":null}]
```

### 질문 유효성 체크 방식 구현

질문이 여행업이 관련된 질문만을 수용하고, 아니라고 판단이 된 질문은 API에서 지정한 답변을 내려주게 되어있다. 이때 질의에서 여행업에 관련된 질의인지 아닌지를 판단한다.

- **[Step 1]** . 추천 질문을 클릭 여부
    - 추천질의는 어드민에서 지정한 질문이므로 체크를 하지 않는다.
- **[Step 2] .** 전처리 이후 빈값 체크
- **[Step 3]** . 형태소 분석 추출
    - 명사, 대명사만을 추출
    - 하나투어 사전을 이용
- **[Step 4]** . 형태소 분석 토큰 결과에 블랙리스트 일치 및 포함 여부
    - 형태소분석에서 분석하지 못하는 경우로 인하여 포함여부를 추가
- **[Step 5]** . 수사값 존재 여부
    - 정규식으로 `수사값 + 박, 일, 인, 명` 의 형태 여부
- **[Step 6]** . 형태소 분석 토큰 결과에 화이트 리스트 일치 및 포함 여부
- **[Step 7]** . 지역 검색 반환 여부
    - 하나투어의 지역 인덱스를 `count 검색`하여 결과가 존재하는지 여부

## 프로젝트 성과

---

### 사용자 로그 분석

`질의/응답`에 대한 결과를 `로그 클러스터에 쌓아서 색인`하도록 하였다. 이때, AI의 답변과 답변 순서를 같이 기록하게 되는데 여러 데이터를 함께 저장하여 `추후 분석`에서 사용할 수 있도록 한다. 

#### 일정을 짜달라는 질문

하나투어는 패키지상품을 주력으로 판매하고 있지만 일정을 짜달라는 질문이 생각보다 많이 들어왔다. 사유는 다음과 같이 분석하고 있다. 

- 젊은사람들이 가지고 패키지 여행 인식
    - 여행은 자유여행으로 직접 일정을 짜서 가야 하는데 일정 짜기에 어려움이 존재한다.
- 패키지 여행의 일정이 마음에 들지 않는다.

#### 질의한 여행 지역 내역

- 질의의 순서가 특정 지역을 연속으로 물어보면 해당 지역으로의 여행을 하고자 한다.
    - 하나투어의 특정 상품을 이미 결정했을 확률이 높다.
- 질의의 지역이 연속적으로 변경되면 마음에 드는 지역 또는 상품을 발견하지 못하여서 고민 중이다.
    - 하나투어 앱을 이탈하여 타 여행사의 상품을 결정할 확률이 존재한다.

다음과 같은 여러 분석 결과 추론을 통해 사용자들이 원하는 형태를 빠르게 반영하도록 한다.

## 트러블 슈팅

---

### 형태소분석 동의어 확장 - **failed to build synonyms**

ES를 통해서 Nori로 형태소분석을 진행하여 `NNP(고유명사)`, `NNG(일반명사)`를 제외한 모든 품사태그를 stoptags에 넣어 단어의 토큰을 빼오도록 설정 했다. 그런데 이때, 동의어를 확장해서 가지고 오려고 하면 예외가 발생 한다.

```json
{
  "error" : {
    "root_cause" : [
      {
        "type" : "illegal_argument_exception",
        "reason" : "failed to build synonyms"
      }
    ],
    "type" : "illegal_argument_exception",
    "reason" : "failed to build synonyms",
    "caused_by" : {
      "type" : "parse_exception",
      "reason" : "Invalid synonym rule at line 3",
      "caused_by" : {
        "type" : "illegal_argument_exception",
        "reason" : "term: guangzhou was completely eliminated by analyzer"
      }
    }
  },
  "status" : 400
}
```

`guangzhou` 라는 단어는 `광저우의 동의어`로 현재 동의어 사전 처리가 되어있다. 

### 발생 원인

`stoptags` 옵션은 분석 결과에서 제거될 단어들을 지정하는 옵션이다. 이 옵션으로 지정된 단어들은 `분석 결과에 포함되지 않는다`. 

> 예를 들어, pos_filter의 stoptags 옵션에 "SL"을 지정하면 외국어(Second Language) 태그가 붙은 단어들은 분석 결과에서 제거
> 

`synonym_filter`에서는 동의어 사전에 있는 `모든 단어들이 분석 결과에 포함`되어야 한다.

`synonym_filter`에서는 이러한 외국어 단어들을 포함한 동의어 사전을 사용하고 있으므로, 이러한 단어들이 `분석 결과에서 제거`되어 버리면서 동의어 필터링이 제대로 동작하지 않는 것.

> 즉, stoptags로 영문을 제거하라고 했는데, 동의어로 지정된 단어가 영문으로 되어있기 때문에 룰이 맞지 않기 때문에 예외가 발생한것.!!
> 

### 해결 방안

`광저우`와 `rhkdwjdn`는 동의어의 관계가 아닌 `오타의 관계`다. 그러나 하나투어에서 초기 구축단계에서 쉽게 구축하기 위해 사전을 분리하지 않고 검색 확장에 대해서 `모두 동의어 사전에 등록`을 하였다. 
현재는 사전을 예외가 발생할 수 있는 `동의어 확장 기능을 제외한 상태`이다.

## 아쉬운 점

해당 프로젝트를 구축하는데 문서가 많질 않아 예상보다 오랜시간이 걸렸다. 또한 구축 이후 Phase 단계별로 코드의 수정도 지속적으로 발생했는데, 막상 정말 중요한 NLP의 영역에서는 제대로 학습을 하는 기회가 오질 않았다. 해서 일반질의에 대한 Validate를 여러가지 고민을해서 구축했지만 실제 정말 중요한 부분에 대한 학습 및 개발 지식을 많이 습득하지 못한 아쉬움이 있다.